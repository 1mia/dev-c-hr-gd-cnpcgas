一：规则拓展
  （1）规则是如何被加载和执行的
          在billruleregister表下
  （2）规则变更后如何生效
          热部署
  （3）规则数据存在哪里
        在billruleregister表下
二：对照代码验证
  （1）宏观理解：这是什么规则，何时触发，核心逻辑是什么，输入输出是什么  ？？？
      这是加班转调休过滤规则。通过在加班明细里将已经转调休的数据取消转调休、然后在考勤日月报里面找到已经取消转调休的数据，点击生成触发。核心逻辑是找到是加班数据里面在过滤规则的数据。输入是加班数据，输出是取消转调休。
  （2）流程梳理
      触发规则，获取加班数据，判断加班数据是否为空，获取过滤规则，判断规则列表是否为空。判断加班数据是否在过滤规则中。
  （3）关键点聚焦
      规则接口规范：命名规范，调用标准接口，执行固定方法，固定返回结果。
                      IYpdCommonRul只有一个execute方法，ruleExecuteRule里可以设置code、message、data
      数据访问模式：单表查询，联表查询。querySheame可以返回pagers，可以根据id返回map.
      业务规则逻辑：有三层判断（判断是否审批通过，判断filtercounter是否大于0）

  （4）问题驱动验证
        规则配置在哪：billruleregister
        拓展性：（如果要新增过滤条件，需要修改哪里）？？？？？

              

  
